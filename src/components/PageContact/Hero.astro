<section class="hero-section">
    <div class="hero-overlay"></div>
    <div class="wrapper">
        <div class="row">
            <div class="contact-header">
                <span class="tag">Свяжитесь с нами</span>
                <h1>Забронируйте свой отдых на берегу Прони</h1>
                <p class="subtitle">
                    На этой странице вы можете узнать о свободных датах,
                    уточнить детали и забронировать понравившийся домик или
                    баню. Ответим на все вопросы и поможем с выбором.
                </p>
            </div>
        </div>
    </div>
</section>

<style lang="scss">
    @use "webcoreui/config" as *;
    @use "webcoreui/styles" as *;

    .hero-section {
        background-image: url("https://opis-cdn.tinkoffjournal.ru/mercury/arenda-doma-in.jpg");
        background-size: cover;
        background-position: center;
        height: 70vh;
        display: flex;
        align-items: center;
        position: relative;
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
            to right,
            rgba(0, 0, 0, 0.7),
            rgba(0, 0, 0, 0.3)
        );
    }
    .row {
        position: absolute;
        top: 50%;
        transform: translateY(-40%);

        max-width: 600px;
        padding: 0 1.5rem;
        color: white;

        .tag {
            display: inline-block;
            background: rgba($green, 0.7);
            // color: #3b82f6;
            padding: 0.5rem 1.25rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.875rem;
            margin-bottom: 1rem;
        }
    }
</style>
